# Final Requirements Cross-Check: Adaptive Fusion Verification

Date: 2026-07-22

| # | Requirement | Evidence type | Evidence | Result |
|---|-------------|---------------|----------|--------|
| 1 | Bind a versioned behavioral verification contract before dispatch, with explicit audited revisions and no silent weakening. | Tests + schema + runtime | `behavioral-verification-contract-schema.json`; `workflow_kernel/behavioral_contract.py`; `test_behavioral_contract.py` mutation/weakening matrix; `test_runtime_cli.py` content-addressed bind, replayed revision, forged retry, stale digest, symlink, and durable-path cases. | PASS |
| 2 | Make validation feedback and browser recovery authoritative, bounded, repeated-failure aware, and fail closed to `human_help_required`. | Tests + contract validators | `workflow_kernel/_translation.py`, `pipeline_adapter.py`, and `retry_policy.py`; `test_pipeline_adapter.py`; `test_retry_policy.py`; existing `test_browser_recovery.py` proves primary quit/restart, distinct alternate engine, exact missing cases, and human-help exhaustion; `validate-routing-economics.sh` and `validate-workflow-contracts.sh` pass. | PASS |
| 3 | Preserve dm-review consensus, unique findings, disagreements, retained/discarded decisions, rationale, raw evidence, and source provenance. | Generated contract + validator + tests | `review-consolidator.md`, review guardrails/output contract, and public dm-review command; `validate-dm-review-codex-perspective.sh` passes; `test_dm_review_adapter.py` covers stable source decisions, disputed literals, duplicate-source rejection, and unsafe references. | PASS |
| 4 | Select extra model perspectives from uncertainty and consequence without changing provider/security authority or slowing the ordinary path. | Policy + exact validators | `routing-policy.json` defines `decisionLeverage` as workflow-depth-only; `promptcraft`, Pipeline commands, and orchestrator consume the profile; hostile selector/authority mutations are rejected by `validate-routing-economics.sh`; workflow-contract validator proves focused ordinary-chunk review remains. | PASS |
| 5 | Record per-attempt/per-model tokens, duration, cost, fallback, unique contribution, retained findings, retries, and human intervention without double counting. | Unit tests + parity tests | `workflow_kernel/metrics.py` and adapter/event translation; 109 focused tests passed for chunk 05; `test_metrics.py` covers complete/partial coverage, overlapping attempts, contribution yield, and intervention identity; `test_shadow_parity.py` treats provider/attempt/provenance mutations as semantic. | PASS |
| 6 | Preserve existing workflow-kernel safety, exact-owned cleanup, persona/browser coverage, provider routing, workflow class, and zero-deferral guarantees. | Release gates + inventory | Generated manifest/alias checks pass (19/33); dependency, dual-compatibility, routing, dm-review, and workflow-contract validators pass; release inventory is exactly 8 schemas and 21 successful CLI commands; all six chunk receipts and terminal reconciliation show 0 created/0 blocked Docker resources. The final 786-test suite has one separately identified live sibling `assembly-baseplate` declaration error and no Depot-owned failure. | PASS WITH EXTERNAL BOUNDARY |

## Verification Boundary

- `./tools/validate-workflow-kernel.py` and therefore the aggregate composition command remain non-green because the live sibling `assembly-baseplate` UX task declaration raises `invalid_verification_declaration`. The chunk prompt explicitly excludes modifying that sibling repository, and the baseline run showed the same boundary.
- The bare prompt command `python3.12 -m unittest tests.test_release_validator` omits Depot's required kernel import path. `PYTHONPATH=plugins/workflow-kernel/skills/workflow-kernel/references python3.12 -m unittest tests.test_release_validator` passes 9/9.
- No UI code changed, so browser execution and screenshots were not applicable to this run; the browser recovery contract and its existing runtime tests were still retained and validated.
