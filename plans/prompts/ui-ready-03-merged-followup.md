# UI-READY-03: finish the existing repair and browser proof

> SUCCESSOR ACTIVE: PR #129 now owns this repair at
> `07c4d5dd6a3efb0b52dc0c4df4bddd42346143dd`. Do not rerun this transfer prompt.
> Inspect the current PR and continue only its remaining evidence/review work.

Repository: `Design-Machines-Studio/depot`
Primary checkout: `/home/ned/ai/depot`
Exact planning base: `bda7cab9ddc97b1bf8746da05ce275584d281031`

executorRole: builder-deep
executorCapabilities: [read-repository, write-repository, tool-use, long-context, structured-output]
executorEffort: high

Resolve roles through installed model-router. Keep concrete identities, rails,
candidate order and costs out of participant packets. Use direct bounded work;
do not restart Pipeline or the original UI-READY implementation.

## Ownership first

This is a continuation for the existing UI-READY owner. Do not launch it as a
second writer while that session is active. If ownership is unknown, perform
read-only inspection and request a handoff before any dependent mutation.

Read AGENTS.md and its references, CLAUDE.md, applicable engineering principles,
the planning index and readiness/resource contracts. Refresh authenticated
GitHub, origin/main, registered worktrees and active repair PRs.

Preserve the primary checkout, all existing worktrees and installed caches.
No stash, reset, clean, rebase, branch switching, file disposal or worktree removal.

The existing worktree is
`/home/ned/ai/depot-worktrees/ui-ready-03-repository-target-discovery`, branch
`fix/ui-ready-03-repository-target-discovery`, HEAD
`f37d790c4155e8417db043b080e1084dc41da758`. At inspection it contains 28 modified
files beyond merged #128, spanning dm-review, Workflow Kernel and their tests/docs.
Do not overwrite or casually adopt this work.

Once ownership is settled, preserve an exact binary diff and status snapshot of
that candidate, including any new files. Create a clean worktree from refreshed
main on `fix/ui-ready-03-compose-proof`; transfer only the reviewed, in-scope
candidate without changing its original worktree. If a successor branch/PR now
owns the repair, continue there instead. Record the actual base and candidate
provenance. Do not absorb the separate planning branch.

## Delivered source and remaining problem

#128 merged at the base above from exact head
`f37d790c4155e8417db043b080e1084dc41da758`; both trees match. It added bounded
repository discovery and source evidence. Exact-head tests passed: discovery
54 assertions and readiness 92. Hosted Codesmith was skipped.

Successful browser delivery remains unproven. Both real target starts failed;
general review was unavailable. The merged helper admits `pre-existing` and
`review-created-process`, while the Fixture consumers use Docker/Compose.
Its public attempt summary also includes argv. The uncommitted follow-up adds
Compose ownership handling, private argv, stricter registry validation and
related tests. Inspect what each change actually solves before retaining it.

No remote `dm-review-v1.80.0` tag exists at planning. Codex's 1.80.0 matches merged
source; Claude remains on 1.79.1. Do not describe this as published consumer proof.
OR-EFFORT-01 is complete; leave model-router 0.6.2 and OpenRouter 1.20.2 unchanged.

## Smallest intended repair

Owner: dm-review. Workflow Kernel participates only where an existing registry
interface cannot support the demonstrated readiness/cleanup handoff.

Finish one working path from a documented repository target through existing
Compose creation and ownership evidence, readiness, desktop/mobile browser
confirmation, and cleanup of review-created resources. Reuse Kernel's current
registry and lifecycle; do not create a competing registry or cleanup authority.

Every retained Kernel change must name this consumer, the demonstrated interface
gap, and the existing complexity it replaces. Reduce the local candidate where
it duplicates a current contract. Do not accept all 28 files merely because they
already exist, or discard useful repairs merely because they are uncommitted.

Keep bounded host interpretation, explicit-target precedence, exact evidence
reuse, dirty-checkout binding, T3-first automation and truthful failure classes.
Keep credential-bearing argv and command output private. Fix all retained
P1/P2/P3 findings without manufacturing new review scope.

Likely surfaces: dm-review's `repository-browser-target-discovery.md`,
`ui-review-readiness.md`/`.sh` and shared host instructions; Kernel's existing
resource registry, Docker adapter, CLI and runtime resolution only as necessary;
their focused tests, canonical manifests, dependency floors and generated docs.

## Non-goals

No model portfolio work, new broker/service/parser/schema family, general
security-hardening program, enterprise workflow features or consumer product
changes. Build for two trusted developers and internally authored Fixtures.
Do not edit consumer dependency pins or release policy to make the canary pass.
Do not stop another session's target or reset/seed persistent developer data.
Do not run another broad review tournament or duplicate completed effort work.

## Acceptance and consumer proof

1. A ready documented Compose target passes through the actual readiness helper
   to host browser confirmation using existing exact resource ownership. Test
   pre-existing reuse, review-created success, failed start, interruption and
   cleanup. No broad Docker cleanup and no synthetic fixture presented as live proof.
2. Documented targets are attempted after their documented prerequisites.
   `visual_target_unavailable` means no declaration; `dev_server_unavailable`
   records an actual command failure or precise missing prerequisite. Browser
   transport failure stays separate. Public results contain no private argv,
   command tails or credential-bearing target material.
3. Existing explicit/T3/structured targets and accepted exact packets continue
   working. A missing structured declaration must not block the documented
   Docker author loop. Do not add a generalized raw-process supervisor.
4. One real exact-head Jig or Governance composition reaches readiness and
   selected meaningful desktop/mobile cases. Read the consumer's setup and
   launcher instructions first. Check documented template generation before
   repeating the prior missing-page-package failure. Read container logs when
   startup fails; a failed curl alone does not establish the cause.
5. Finish the missing bounded general review and fix all retained findings.
   Reuse prior evidence only when still exact for the affected files and behavior.

Consumer heads at planning:
- Jig: `50f0d47c275928953dcaa81ee15d68e05cf0a0ae`.
- Governance: `4777a292bd4ea52b74bbbc0c82be1524ca0f0299`; PR #42 is active.
- Baseplate main: `40dc3cb8d189bc1cdce9a8189822920e4596a03f`; product PR #856
  and planning PR #847 are active.
- Governance's declared Baseplate alpha.16:
  `7fa3f108604a2e604e4dc99af5fb96d90b5ae307`.

Refresh these before use. Prefer an isolated Jig canary if that avoids active
Governance work; retain each repository's declared compatibility. Jig documents
`make -C "$BASEPLATE_DIR" templ-generate` before its setup/author loop. Use exact
documented commands and dynamic URLs, unique instances and disposable data.
Record Depot/Fixture/Baseplate heads, source state, generation/start/readiness
commands, routes/personas, desktop/mobile evidence and owned cleanup.

If a consumer still fails after its documented prerequisites, provide its owner
a precise reproduction and preserve the honest gap. Do not repair consumer code
in Depot. Keep Jig's portable declaration handoff concise and consumer-owned.

## Verification, versions and review

Run meaningful readiness/discovery tests against the actual helper and registry,
not only prose assertions. Run affected shell syntax checks, existing UI-contract
and Pipeline browser-evidence suites, workflow contract checks, dependencies,
dual compatibility, generated-file checks and `git diff --check`.

If Kernel changes survive, run its affected registry/CLI/adapter tests through
the supported repository runner and `./tools/validate-workflow-kernel.py`.
Run `./tools/validate-composition.sh --all` on the final candidate. Diagnose
failures against the exact base; do not weaken tests to accept the candidate.

Review request: executorRole `review-fast`, executorCapabilities
`[read-repository, long-context, structured-output]`, executorEffort `medium`.
Use one focused `security-review` with the same capabilities/effort only for
changed credential output or executable cleanup authority; do not rerun unchanged
converged lanes. Route independently and report unavailable review honestly.

Use economical metered delegation when appropriate, capped at $0.25 total for
this chunk within the user's $50/month target. Check available usage evidence;
do not invent aggregate monthly headroom or change account limits.

Expected version movement: dm-review 1.80.0 -> 1.80.1. It already merged and has
an installed copy, so do not publish changed bytes under its existing version.
The local Kernel candidate is 0.20.0; retain that minor bump and dm-review's
>=0.20.0 floor only if the new required Kernel interface survives scope review.
Reacquire versions/collisions and explain any necessary difference.

Edit canonical Claude plugin and marketplace manifests. Regenerate Codex
manifests/aliases and affected indexes/dependency docs; never hand-edit generated
surfaces. Do not bump unrelated plugins or raise unrelated dependency floors.

## Delivery and release boundary

Commit and push the verified follow-up; open one successor PR targeting main,
linked to merged #128. Keep it draft if required review or live proof is missing.
Do not create/close/reassign native Issues, reopen #128 or substantially rewrite
unrelated PRs. Preserve original worktrees and their handoff evidence.

Project 1 is the existing Assembly Coordination project. Leave merged #128's
Done/P1/Tooling item as source history. Add only the actual successor PR as
P1/Tooling: In progress while executing, Review once proof is complete, or
Blocked with the exact dependency. No free-form backlog card or second Project.

Do not merge, tag or synchronize installations in this execution. Prepare the
concrete release closeout: exact-head proof, trusted merged-main validation,
release preflight, approved dm-review and any required Kernel tags, both Claude
and Codex cache synchronization/comparison, then installed-consumer browser proof.
Expected tags are `dm-review-v1.80.1` and, only if required by the retained
interface, `workflow-kernel-v0.20.0`; refresh actual versions before publication.
Run preflight and report cache/version coverage gaps accurately; an installed
old version is pending release work, not permission to alter caches silently.
Tagging/merging/cache synchronization require separate explicit authorization.

At approximately 40 tool calls, stop new research and scope expansion. Continue
focused repair, verification, commit, push and PR handoff. Do not abandon completed
work uncommitted or unpushed because of the checkpoint.

Return exact source/repair heads, ownership and branch, PR/checks, Project state,
retained/dismissed findings, local/trusted-main/publication/cache/consumer evidence
separately, and one next action. Include `SIMPLICITY-CHECK`, `NOT-COVERED` and
`COMMANDS-RUN` with exits. Finish with one compact model-and-cost report: role,
attempted/served participant, rail, requested/transmitted effort, duration,
outcome, tokens, measured paid cost, subscription calls, fallbacks and unavailable
measurements. Request transmission is not internal reasoning measurement.
