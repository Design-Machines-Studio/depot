{
  "_comment": "Single provider-routing policy shared by promptcraft, execution-orchestrator, cascade-dispatch, dm-review, assess, and research. Keep task-fit decisions here so provider routing does not drift across plugin docs.",
  "subscriptionFirst": {
    "invariant": "subscription rails first while live headroom clears the threshold for BOTH the weekly cap and the 5-hour window; API rails next by quality-per-price from the model matrix; never start planned multi-chunk work that projected spend would push below threshold mid-run",
    "headroomSource": "usage-probe.sh live probe; a subscription rail is available only when its record is well-formed, state is exactly ok, and numeric remaining_pct is above the threshold. Unknown, missing, malformed, at-threshold, limited, and low subscription readings are unavailable. OpenRouter is an auto-reloading API balance rail: it is available only when state is exactly ok and balance_usd is a finite non-negative number; it has no legacy $5 minimum.",
    "thresholdRef": "model-cascade.json policy.headroom_threshold_pct"
  },
  "operatorProfile": {
    "path": ".dm/operator-profile.local.json",
    "schema": "operator-profile-schema.json",
    "precedence": "The profile may RANK and REMOVE only among policy-derived routing options; it never adds a routing provider the derivation did not find, and never overrides neverOfferable, security invariants, or the family-independence rule. Declaring a subscription rail is NOT adding a routing option: a profile entry supplies observability only -- which windows a rail has and how to probe its headroom. A declared rail becomes eligible to route only if policy derivation already found that provider; if it did not, the entry still yields headroom readings and still routes nothing.",
    "precedenceRef": "operator-profile-schema.json properties.precedence -- the tracked definition of remove-only precedence",
    "absence": "No profile present means policy defaults and today's behavior.",
    "subscriptionSet": "Open for OBSERVABILITY only: declare a new subscription rail with one profile entry, its windows, and its probe argv, and usage-probe.sh will report that rail's headroom with no contract edit. That alone can NEVER make the rail routable -- adding a routing option still requires a policy-ladder or derivation change, per precedence above. A profile entry for a provider the derivation did not find yields headroom readings and routes nothing.",
    "probeResolution": "Use the built-in probe for a known rail, otherwise the profile-declared probe, otherwise unknown. Every profile probe runs under usage-probe.sh's reset PATH and must map to the common state, remaining_pct, and window shape; unmappable output is unknown.",
    "dataBoundary": "Profiles carry preferences and probe commands only, never credentials or account secrets.",
    "consumerStatus": "usage-probe.sh consumes .subscriptions only -- it executes each rail's probe and emits headroom. familyPreferenceOrder and neverUse are validated for shape but are NOT yet read by any executable consumer: cascade-dispatch.sh selects rails from the policy ladder and does not load this profile. Until a selection-time consumer exists, a profile can shrink or reorder nothing at runtime, and an operator MUST NOT assume neverUse removes a provider from dispatch. The declared contract is the target shape; this field records how much of it is live, so the gap is visible rather than implied.",
    "probeExecution": "A profile probe is an argv ARRAY, never a shell string, and is executed without sh -c: no metacharacters, word splitting, globbing, or chaining. probe[0] must resolve on the reset PATH or be an absolute path to an executable regular file. A git-TRACKED profile is refused outright, because .gitignore is defeated by git add -f in this repository and a committed profile is not developer-local config. Symlinked profiles are refused."
  },
  "targets": {
    "activeSubscriptionProfile": "codex-20x",
    "subscriptionProfiles": {
      "codex-20x": {
        "codex": 65,
        "claude": 0,
        "openrouter": 35
      },
      "codex-5x": {
        "codex": 40,
        "claude": 0,
        "openrouter": 60
      }
    },
    "enforcement": {
      "scope": "eligible-chunks-per-run",
      "strategy": "deficit-round-robin",
      "flexibleBuckets": [
        "config",
        "docs",
        "mechanical-logic"
      ],
      "fixedBuckets": [
        "logic",
        "ui",
        "integration"
      ],
      "securityOverridesTarget": true,
      "toolCapabilityOverridesTarget": true,
      "varianceReceiptRequired": true
    },
    "rationale": "Coding-only calibration: subscriptionProfiles[activeSubscriptionProfile] is the sole target. Codex Pro 20x uses 65/0/35 Codex/Claude/OpenRouter; switching to codex-5x uses 40/0/60. Claude is non-coding-only and therefore has zero coding share. Apply the target as routing pressure only among eligible chunks; security and required live-tool capabilities remain authoritative and their exclusions must be receipted. DeepSeek V4 usage is counted under OpenRouter."
  },
  "decisionLeverage": {
    "scope": "workflow-depth-only",
    "allowedLevels": [
      "low",
      "medium",
      "high"
    ],
    "legacyDefault": {
      "planningDepth": "current-standard-path",
      "verificationDepth": "current-standard-path",
      "receiptFlag": "decision_profile_defaulted=true",
      "semanticClaim": "unknown-not-low-low"
    },
    "rules": {
      "lowLow": {
        "when": {
          "uncertainty": "low",
          "consequence": "low"
        },
        "planningDepth": "standard",
        "verificationDepth": "standard",
        "optimized": true
      },
      "highUncertainty": {
        "when": {
          "uncertainty": "high"
        },
        "planningDepth": "one-independent-opinion-plus-bounded-synthesis",
        "verificationDepth": "standard-unless-high-consequence",
        "optimized": false
      },
      "highConsequence": {
        "when": {
          "consequence": "high"
        },
        "planningDepth": "standard-unless-high-uncertainty",
        "verificationDepth": "stronger-existing-independent-seam",
        "optimized": false
      },
      "highHigh": {
        "when": {
          "uncertainty": "high",
          "consequence": "high"
        },
        "planningDepth": "one-independent-opinion-plus-bounded-synthesis",
        "verificationDepth": "stronger-existing-independent-seam",
        "optimized": false
      }
    },
    "invariants": [
      "routing-unchanged",
      "safety-gates-unchanged",
      "browser-and-persona-coverage-unchanged",
      "workflow-class-unchanged",
      "cleanup-unchanged",
      "economics-unchanged",
      "no-debate-or-per-chunk-full-review"
    ]
  },
  "chunkKind": {
    "_comment_kinds": "config/docs/mechanical-logic/logic/ui/integration are ROUTING buckets, not manifest kind enum values. The manifest kind enum stays {ui, logic, integration, config}; renderedSurface is a separate visual/browser applicability field and never changes routing. Docs and mechanical-logic are refinements carried by the manifest `executor` field (derived from kind + estimatedComplexity per manifest-schema.md Executor Mapping), not first-class kinds. model-cascade.class_from_kind bridges only the manifest kinds (logic/config/docs/ui/integration); mechanical-logic routes via `executor: openrouter` set at prompt-generation time, so it needs no class_from_kind entry.",
    "config": {
      "provider": "openrouter",
      "cascadeClass": "openrouter",
      "rationale": "Docs, receipts, JSON/YAML/TOML, and pure-prose edits fit cheap large-context text models."
    },
    "docs": {
      "provider": "openrouter",
      "cascadeClass": "openrouter",
      "rationale": "Documentation generation and cleanup is text-heavy and usually low visual-risk."
    },
    "mechanical-logic": {
      "provider": "openrouter",
      "cascadeClass": "openrouter",
      "rationale": "Rename follow-through, test tables, seed data, and migration edits are bounded mechanical code changes."
    },
    "logic": {
      "provider": "codex",
      "cascadeClass": "codex",
      "rationale": "Complex service and handler logic uses Codex agentic coding with OpenRouter fallback."
    },
    "ui": {
      "provider": "codex",
      "cascadeClass": "codex",
      "rationale": "UI implementation and code review use Codex with browser evidence; Claude is reserved for non-coding work."
    },
    "integration": {
      "provider": "codex",
      "cascadeClass": "codex",
      "rationale": "Cross-chunk wiring and route verification use Codex, with OpenRouter fallbacks and no Claude coding rung."
    }
  },
  "agentType": {
    "pattern-recognition-specialist": {
      "provider": "openrouter",
      "model": "openai/gpt-5.6-luna",
      "fallbackModel": "openai/gpt-5.6-terra",
      "fallbackProvider": "codex",
      "rationale": "Pattern checks use low-cost Luna on the OpenRouter rail, then Terra, before Codex fallback."
    },
    "code-simplicity-reviewer": {
      "provider": "openrouter",
      "model": "openai/gpt-5.6-luna",
      "fallbackModel": "openai/gpt-5.6-terra",
      "fallbackProvider": "codex",
      "rationale": "Simplification review uses low-cost Luna, then Terra, before Codex fallback."
    },
    "doc-sync-reviewer": {
      "provider": "openrouter",
      "model": "openai/gpt-5.6-luna",
      "fallbackModel": "openai/gpt-5.6-terra",
      "fallbackProvider": "codex",
      "rationale": "Documentation consistency uses OpenRouter large-context models at any diff size."
    },
    "test-coverage-reviewer": {
      "provider": "openrouter",
      "model": "openai/gpt-5.6-luna",
      "fallbackModel": "openai/gpt-5.6-terra",
      "fallbackProvider": "codex",
      "rationale": "Coverage gap detection is read-heavy and uses the OpenRouter mechanical ladder."
    },
    "openrouter-bulk-analyst": {
      "provider": "openrouter",
      "model": "moonshotai/kimi-k3",
      "fallbackModel": "openai/gpt-5.6-terra",
      "fallbackProvider": "codex",
      "rationale": "Bulk/large-context analysis keeps Kimi K3 primary for provider diversity, with Terra then Codex as capacity fallbacks."
    },
    "security-auditor-openrouter": {
      "provider": "openrouter",
      "model": "moonshotai/kimi-k3",
      "fallbackModel": "openai/gpt-5.6-terra",
      "fallbackProvider": "codex",
      "rationale": "Kimi K3 analyzes only content-boundary-eligible sections; full or partial declines are completed under this same logical lane on Codex."
    },
    "security-auditor-codex-signoff": {
      "provider": "implementer-aware-independent-family",
      "preferredProviderWhenIndependent": "codex",
      "codexImplementerProvider": "openrouter",
      "codexImplementerModel": "moonshotai/kimi-k3",
      "codexImplementerFallbackModel": "openai/gpt-5.6-terra",
      "required": true,
      "inputScope": "full-diff",
      "reviewerFamilyConstraint": "must-differ-from-implementer-family",
      "failureResolution": {
        "runner_failure": "remaining-non-implementing-family-or-review-incomplete",
        "full_disclosure_decline": "remaining-non-implementing-family-or-review-incomplete",
        "partial_coverage": "remaining-non-implementing-family-or-review-incomplete"
      },
      "rationale": "Independent full-diff security completion is mandatory. The stable lane id does not select Codex when Codex implemented the diff."
    },
    "architecture-reviewer": {
      "provider": "codex",
      "rationale": "Architecture code review is Codex-native; Claude is non-coding-only."
    }
  },
  "phase": {
    "assessFanout": {
      "provider": "openrouter",
      "env": "ASSESS_EXECUTOR",
      "fallbackProvider": "claude",
      "rationale": "Assessment fan-out is read-heavy summarization; keep final synthesis on Claude."
    },
    "researchFanout": {
      "provider": "openrouter",
      "env": "RESEARCH_EXECUTOR",
      "fallbackProvider": "claude",
      "rationale": "Research fan-out is read-heavy summarization; keep final synthesis on Claude."
    },
    "adversarialReview": {
      "providers": [
        "codex",
        "openrouter"
      ],
      "optionalProvider": "claude",
      "optionalEnv": "PIPELINE_CLAUDE_ADVERSARY",
      "rationale": "Default adversarial review uses two non-Claude lenses; Claude plan-adversary is an optional third lens or tiebreak."
    }
  },
  "exhaustionFallback": {
    "_comment": "Fires when every configured rail for a chunk is exhausted or gated (cascade RC 76). The operator may wait or park; there is no hidden fallback rail. PIPELINE_EXHAUSTION_ASK=0 selects resumable parking directly for headless CI.",
    "mode": "ask",
    "headlessDefault": "park",
    "neverOfferable": [
      "openrouter_automated_lanes",
      "any provider for sensitive-path chunks"
    ],
    "failClosed": "a missing routing-policy.json, a missing or malformed exhaustionFallback, or any mode other than ask blocks execution rather than selecting an undeclared rail; an explicit valid headless configuration parks resumably",
    "liveStatusRequired": "the ask shows the derived status per rail (capped-until / available / unknown) so the operator decides from live facts; where usage-probe parsers are TODO stubs the ask says unknown rather than guessing",
    "rationale": "Wait or park preserves resumable state when the configured Codex and OpenRouter rails are unavailable."
  },
  "invariants": {
    "crossProviderVerification": true,
    "finalReviewDifferentProvider": true,
    "reviewerFamilyIndependent": true,
    "proposalOnlyEconomics": true,
    "providerOrigin": {
      "schemaVersion": 1,
      "openrouterForbiddenModelPrefixes": [
        "anthropic/"
      ],
      "nativeOpenAIExecution": "codex-cli-preferred-or-openrouter-api",
      "nativeAnthropicExecution": "claude-cli-only",
      "genericNativeVendorFallback": "unavailable",
      "appliesTo": [
        "primary",
        "fallback"
      ],
      "failureMode": "fail-closed-before-provider-contact",
      "rationale": "Provider origin is receipted explicitly. OpenAI and third-party OpenRouter models remain eligible after disclosure and output controls; Anthropic remains native-only."
    }
  },
  "matrixReceipt": {
    "_comment": "Receipt contract for model selection. The canonical machine-readable model data lives in plugins/openrouter/skills/openrouter-delegate/references/model-matrix.json; tools/validate-openrouter-cascade.sh rejects drift between that matrix, model-cascade.json quality_rank, and the agentType slugs in this file. Same canonical-with-checked-mirror shape as .security above.",
    "canonicalMatrix": "../../openrouter/skills/openrouter-delegate/references/model-matrix.json",
    "statement": "Every routing decision recorded in a run receipt must carry matrix_snapshot_date and a one-line rung_rationale naming the deciding axis (cost, context, strength, or availability).",
    "requiredReceiptFields": {
      "matrix_snapshot_date": "The snapshot_date of the model-matrix.json revision the decision was made against.",
      "rung_rationale": "One line naming the deciding axis for the chosen rung."
    },
    "rungRationaleAxes": [
      "cost",
      "context",
      "strength",
      "availability"
    ],
    "appliesTo": [
      "chunkKind",
      "agentType",
      "phase",
      "cascade rung selection"
    ],
    "failureMode": "receipt-incomplete",
    "authority": "The matrix recommends, this policy decides, receipts record. A matrix refresh never re-routes anything on its own.",
    "rationale": "Routing decisions were previously unattributable: the matrix was consumed by nothing at run time, so a receipt could not show which price/context/strength data a rung was chosen against. Naming the snapshot and the deciding axis makes the decision auditable and makes matrix drift visible in the receipt trail."
  },
  "authorization": {
    "schemaVersion": 2,
    "_comment": "Configured key plus a coherent installed bundle makes OpenRouter available for eligible payloads. Exact outbound bytes are screened automatically immediately before wrapper invocation. Broker status is not consulted.",
    "resolutionOrder": ["configured_key_and_bundle", "automatic_payload_screen", "native_fallback"],
    "modes": {
      "trusted_boundary": {
        "status": "active",
        "keyInputs": ["OPENROUTER_API_KEY", "OPENROUTER_API_KEY_FILE"],
        "bundleRequirement": "one coherent installed OpenRouter bundle",
        "payloadRequirement": "one automatic disclosure scan over private system/user files immediately before sending those same files",
        "receiptFields": ["provider", "model", "usage", "requestEnvelopeSha256"],
        "brokerIndependent": true,
        "costControl": "provider-side per-key spending limits"
      },
      "unavailable": {
        "status": "fallback",
        "reason": "configured_key_bundle_provider_or_payload_unavailable",
        "exhaustionFallback": "codex",
        "_comment": "Missing or invalid credentials, an incoherent bundle, provider failure, or an automatically declined payload returns to Codex without prompting."
      }
    }
  },
  "security": {
    "_comment": "Validated mirror of the OpenRouter-owned delegation policy. The canonical installed-runtime source is plugins/openrouter/skills/openrouter-delegate/references/delegation-security-policy.json. This block keeps pipeline planning self-contained and tools/validate-routing-economics.sh rejects drift.",
    "delegationSecurityPolicy": "../../openrouter/skills/openrouter-delegate/references/delegation-security-policy.json",
    "disclosureControls": {
      "refuseClasses": [
        "high-confidence credentials",
        "private keys",
        "authenticated connection strings / DSNs",
        "access or session tokens",
        "explicitly classified private or regulated values"
      ],
      "placeholderValues": [
        "<token>",
        "<redacted>",
        "REDACTED",
        "example",
        "changeme",
        "${VARIABLE}",
        "$VARIABLE",
        "..."
      ],
      "onMatch": "decline-disclosure",
      "exitCode": 3,
      "rationale": "Classify the exact outbound bytes. Names of files, directories, headers, variables, vendors, nationalities, and jurisdictions are not disclosure evidence."
    },
    "executionControls": {
      "requirements": [
        "non-empty validated unified diff",
        "normalized relative paths",
        "exact complete owned-path allowlist",
        "no symlink escape",
        "trusted caller-supplied verification",
        "allowlist-only staging"
      ],
      "modelCommandAuthority": "none",
      "malformedInput": "fail-closed"
    },
    "delegationModes": {
      "execution": {
        "contentValidation": "exact-outbound-prompt-and-complete-diff",
        "outputAuthority": "validated-unified-diff-within-owned-paths"
      },
      "mechanical-review": {
        "contentValidation": "complete-diff-partitioned-by-file-section-including-removed-lines",
        "sensitiveSectionAction": "exclude-section-and-require-codex-coverage",
        "emptyRemainder": "decline-disclosure",
        "outputAuthority": "review-text-only"
      },
      "artifact-review": {
        "contentValidation": "exact-artifact-content",
        "pathReferences": "allow",
        "outputAuthority": "review-text-only"
      },
      "artifact-delegation": {
        "contentValidation": "multiple-explicit-text-files-exact-bytes",
        "pathReferences": "allow",
        "outputAuthority": "none",
        "executionPathAuthority": "none"
      }
    },
    "reviewControls": {
      "highConsequenceSecuritySignoff": "independent-non-implementing-family-required",
      "modelNationalityEmbargo": false,
      "vendorJurisdictionEmbargo": false,
      "pathNameEmbargo": false,
      "rationale": "Security-related implementation may be delegated after content and output validation; consequential security completion requires a reviewer family different from the implementer."
    },
    "positiveRouting": {
      "intendedLanes": [
        "security analysis with independent non-implementing-family sign-off",
        "style / formatting",
        "duplication",
        "pattern-recognition",
        "large-diff first-pass triage",
        "doc consistency"
      ],
      "rationale": "These are the OpenRouter mechanical lanes when exact outbound content passes disclosure controls."
    }
  }
}
