{
  "_comment": "Kernel-owned safety data for paths that never route to OpenRouter. Sensitive coding remains Codex-native. This installed copy is synchronized with routing-policy.json security.neverRouteToOpenRouter.pathGlobs.",
  "security": {
    "neverRouteToOpenRouter": {
      "pathGlobs": [
        "internal/auth/**",
        "internal/federation/**",
        "**/secretbox*",
        "**/destructive_confirmation*",
        "internal/baseplate/email/settings*",
        "deploy/**",
        "*.env*"
      ]
    }
  }
}
