# Independent-family lanes

Callers pass opaque private receipt IDs, never family names. model-router reads
the private evidence, excludes every implementing family, and returns only an
anonymous participant and pass/fail disposition. Mixed implementation passes
all contributing receipt IDs. A missing receipt or exhausted independent role
keeps the lane incomplete; dm-review cannot weaken or reinterpret the result.
