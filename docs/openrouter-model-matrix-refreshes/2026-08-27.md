# OpenRouter model matrix refresh — 2026-08-27

This is the compact catalog evidence receipt for the 2026-08-27 controlled
benchmark review. Eight exact-model attempts across GLM-5.3-Flash and DeepSeek
V4 Pro 0813 were retained under an explicit operator spend override. A DeepSeek
control exposed and verified a third harness repair. Three fully corrected
DeepSeek attempts passed, but the single-case evidence does not justify a
role-policy change.

## Catalog receipt

- Source: `https://openrouter.ai/api/v1/models`
- Observed: `2026-08-27T13:57:25+08:00`
- Expires: `2026-08-27T14:12:25+08:00`
- Models observed: 417
- SHA-256: `6b22e89ea31924f3d68c4a6d799122472bfeb0abbe835f4d7455b7f930e99839`

Every pre-existing matrix slug remained present. The final fresh catalog had
the same digest and no additional material change. It confirmed the earlier
same-day changes already recorded:

- `deepseek/deepseek-v4-flash-0731`: input/output/cache-read prices rose from
  $0.04/$0.08/$0.008 to $0.06/$0.12/$0.012 per million tokens.
- `z-ai/glm-5.3-flash`: reasoning is mandatory and defaults to `max`; supported
  efforts are `max`, `high`, and `low`; default temperature/top-p are 1/0.95.

## Candidate and benchmark status

The catalog nominated `qwen/qwen3.8-flash` and `z-ai/glm-5.3-flash` as new
candidates. Only the user-selected GLM model was admitted to the matrix. The
former Ox Alpha stealth identity has been revealed as GLM-5.3-Flash, so the
live exact slug `z-ai/glm-5.3-flash` and canonical identity
`z-ai/glm-5.3-flash-20260826` are recorded without transferring any score.

The model supports the suite's text, reasoning, and JSON-response needs in the
catalog. The existing key still reported null provider-limit fields, but the
user explicitly authorized the configured key with local measured-spend
guardrails. `pipeline-legacy-translation` attempts 1 and 2 failed with HTTP 429
because the benchmark incorrectly disabled same-model provider fallback. That
tooling defect was fixed in OpenRouter 1.19.5 while model fallback remained
disabled by the single exact slug.

Attempt 3 reached BaseTen after that repair but exposed a second tooling defect:
the prompt omitted the exact mapping required by its deterministic scorer. The
attempt was retained as parsed=false, quality 0, and cost $0.00012475, but it is
not comparable evidence. OpenRouter 1.19.6 made the case self-contained.

Attempt 4 reached Modal with the exact requested model, no model fallback, and
same-model provider fallback enabled. It parsed and scored 15/100 in 16 seconds,
but a DeepSeek control later proved the prompt still described one `chunks`
entry without explicitly requiring the top-level `chunks` envelope. The GLM
attempt is retained but is not comparable evidence. No GLM attempt remains
after all three harness repairs.

The selected control was the live, already-catalogued exact identity
`deepseek/deepseek-v4-pro-0813`. Its first attempt reached BaseTen, reproduced
the same direct-chunk response, and scored 15/100 for $0.00230076 before the
envelope repair. OpenRouter 1.19.8 made the top-level envelope explicit and
added a regression assertion. Three subsequent exact-model attempts reached
Fireworks twice and Parasail once, used no model fallback, parsed, and each
scored 100/100. Their comparable medians were 6 seconds, 163 prompt tokens, 258
completion tokens, 195 reasoning tokens, and $0.00123684 provider-billed cost.
Comparable DeepSeek spend was $0.00387684; total DeepSeek spend including the
retained pre-fix control was $0.0061776. Total measured benchmark spend across
both models was $0.006607669465. The remaining three cases and native incumbents
were not run.

## Routing decision

No policy change. GLM-5.3-Flash is catalogued and not routed because it has no
attempt after all three harness repairs; gates 2, 3, 5, 6, and 7 remain unmet.
DeepSeek V4 Pro 0813 clears the three-attempt quality, fallback, duration, token,
and billed-cost floor for `pipeline-legacy-translation`, but gate 2 is incomplete
for every other applicable case, gate 3 has only single-case coverage, gate 5
has no proven advantage over an included-subscription incumbent, and gate 6
lacks five attributable production-canary attempts. Its existing role order is
unchanged.

## Production evidence

The production report retains four Kimi K3 attempts costing $2.8345398 and one
GLM-5.2 attempt costing $0.1614712. Median completion is 1.0, median fallback
is 0.1, and median first-pass validation is 0.0. Two of three cost summaries
are empty, there are no canonical finding contributions, and the artifacts do
not support isolated model-quality attribution.

## Source notes

OpenRouter's current model page states that Ox Alpha was revealed as ZAI
GLM-5.3-Flash. The catalog snapshot remains the authority for exact executable
identity, pricing, context, capabilities, and provider limits. No external
benchmark score was transferred from another model, version, or harness. The
generic aggregate report groups by provider and cannot mark harness-version
comparability; this note and the private plan distinguish the retained pre-fix
attempts from the three comparable DeepSeek results.
