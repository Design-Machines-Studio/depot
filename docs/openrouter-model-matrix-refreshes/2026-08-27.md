# OpenRouter model matrix refresh — 2026-08-27

This is the compact catalog evidence receipt for the 2026-08-27 controlled
benchmark review. Four exact-model attempts were retained under an explicit
operator spend override. Tooling repairs converted the initial transport
failures into valid inference, but the corrected quality screen did not justify
a role-policy change.

## Catalog receipt

- Source: `https://openrouter.ai/api/v1/models`
- Observed: `2026-08-27T13:00:19+08:00`
- Expires: `2026-08-27T13:15:19+08:00`
- Models observed: 417
- SHA-256: `6b22e89ea31924f3d68c4a6d799122472bfeb0abbe835f4d7455b7f930e99839`

Every pre-existing matrix slug remained present. The fresh retry catalog
confirmed the earlier same-day material changes already recorded:

- `deepseek/deepseek-v4-flash-0731`: input/output/cache-read prices rose from
  $0.04/$0.08/$0.008 to $0.06/$0.12/$0.012 per million tokens.
- `z-ai/glm-5.3-flash`: reasoning is mandatory and defaults to `max`; supported
  efforts are `max`, `high`, and `low`; default temperature/top-p are 1/0.95.

## Candidate and benchmark status

The catalog nominated `qwen/qwen3.8-flash` and `z-ai/glm-5.3-flash` as new
candidates. Only the user-selected GLM model was admitted to the matrix. The
former Ox Alpha stealth identity has been revealed as GLM-5.3-Flash, so the
live exact slug `z-ai/glm-5.3-flash` and canonical identity
`z-ai/glm-5.3-flash-20260826` are recorded without transferring any score.

The model supports the suite's text, reasoning, and JSON-response needs in the
catalog. The existing key still reported null provider-limit fields, but the
user explicitly authorized the configured key with local measured-spend
guardrails. `pipeline-legacy-translation` attempts 1 and 2 failed with HTTP 429
because the benchmark incorrectly disabled same-model provider fallback. That
tooling defect was fixed in OpenRouter 1.19.5 while model fallback remained
disabled by the single exact slug.

Attempt 3 reached BaseTen after that repair but exposed a second tooling defect:
the prompt omitted the exact mapping required by its deterministic scorer. The
attempt was retained as parsed=false, quality 0, and cost $0.00012475, but it is
not comparable evidence. OpenRouter 1.19.6 made the case self-contained.

The fully corrected attempt 4 reached Modal with the exact requested model, no
model fallback, and same-model provider fallback enabled. It parsed but scored
15/100 in 16 seconds: 139 prompt tokens, 569 completion tokens, no reported
reasoning-token count, and provider-billed cost $0.000305319465. The model
returned the mapped chunk directly instead of the required `chunks` array and
retained `kind`. Total measured benchmark spend was $0.000430069465. The other
three cases were skipped after the corrected screen missed the quality floor.

## Routing decision

No policy change. GLM-5.3-Flash is catalogued with four retained attempts and is
not routed. Gate 2 fails because only one corrected comparable attempt exists
and its 15/100 score is below 90. Gate 3 fails because multi-case duration,
token, and cost coverage is absent. Gates 5, 6, and 7 fail because no advantage
over subscription incumbents, production canary evidence, or exact evidence
reversing the existing GLM constraint exists.

## Production evidence

The production report retains four Kimi K3 attempts costing $2.8345398 and one
GLM-5.2 attempt costing $0.1614712. Median completion is 1.0, median fallback
is 0.1, and median first-pass validation is 0.0. Two of three cost summaries
are empty, there are no canonical finding contributions, and the artifacts do
not support isolated model-quality attribution.

## Source notes

OpenRouter's current model page states that Ox Alpha was revealed as ZAI
GLM-5.3-Flash. The catalog snapshot remains the authority for exact executable
identity, pricing, context, capabilities, and provider limits. No external
benchmark score was transferred from another model, version, or harness. The
tooling repairs are retained independently from the model-quality result.
